# Install skill

Read **`.cursor/skills/artspectiv-skill-install/SKILL.md`** and follow the vendor → review → `.cursor/skills/` flow.

## Quick reference

```bash
npx skills add <source> --skill <id> -y -g -a cursor
```

Update `.cursor/skills/skills-sh-curation.md` and run:

```bash
npm run audit:skills
npm run verify:agent-harness
```
